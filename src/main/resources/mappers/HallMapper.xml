<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.javawebdemo.mapper.HallMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO halls (name, seat_total, hall_type, seat_layout_json)
        VALUES (#{name}, #{seatTotal}, #{hallType}, #{seatLayoutJson})
    </insert>

    <update id="update">
        UPDATE halls
        SET name = #{name},
            seat_total = #{seatTotal},
            hall_type = #{hallType},
            seat_layout_json = #{seatLayoutJson}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM halls WHERE id = #{id}
    </delete>

    <select id="findById" resultType="Hall">
        SELECT * FROM halls WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Hall">
        SELECT * FROM halls ORDER BY created_at DESC
    </select>
</mapper>
