<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.javawebdemo.mapper.OrderMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, user_id, show_id, total_price, status)
        VALUES (#{orderNo}, #{userId}, #{showId}, #{totalPrice}, #{status})
    </insert>

    <select id="findById" resultType="Order">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultType="Order">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>

    <select id="findByUserId" resultType="Order">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findAllWithFilters" resultType="Order">
        SELECT * FROM orders
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status},
            paid_at = #{paidAt},
            refunded_at = #{refundedAt}
        WHERE id = #{id}
    </update>

    <update id="cancelExpired">
        UPDATE orders
        SET status = 'CANCELED'
        WHERE status = 'PENDING' AND created_at &lt;= #{before}
    </update>
</mapper>
